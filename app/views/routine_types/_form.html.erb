<%= form_for(routine_type) do |f| %>
  <% if routine_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(routine_type.errors.count, "error") %> prohibited this routine_type from being saved:</h2>

      <ul>
      <% routine_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
